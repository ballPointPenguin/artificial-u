Finding this to work ok:

```bash
python scripts/normalize_audio_pedalboard.py --ratio 8.0 --threshold -18 --attack 3.0 audio/NEURO250_1_1.mp3
```

That is heavy compression (8.0) with a low-ish threshold and fast attack.

Would like to use less compression perhaps. Pedalboard works decently well.

More research is needed.
